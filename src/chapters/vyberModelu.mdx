import { ImageWithCaption } from "../components/ImageWithCaption";

<article id="vyberModelu">
  <header style={{marginBottom: "1rem"}}>
    ##  Výběr modelů 🔍
  </header>

Pro náš projekt jsme zvolili tři populární modely – **RandomForestClassifier**, **XGBClassifier** a **SVC**. Tyto modely jsme zvolili, jelikož se jedná o různé typy modelů a chtěli jsme tak zjisit, který bude lepší pro náš dataset a rozhodovací problém.

**RandomForestClassifier**: Tento model spadající pod ensemble modely byl zvolen pro jeho schopnost dobře zvládnout velké množství vstupních proměnných a jeho robustnost vůči přetrénování. RandomForest je také známý tím, že dokáže dobře zvládnout nevyvážené datasety a je velmi flexibilní v tom, jaký typ dat dokáže zpracovat.

**XGBClassifier**: XGBoost je gradient boosting model, který je známý svou rychlostí a výkonností. XGBoost také nabízí řadu možností pro ladění a má vestavěné funkce pro regulaci modelu a prevenci přetrénování.

**SVC (Support Vector Classifier)**: Tento model byl zvolen pro svou schopnost efektivně zpracovávat i vysoce dimenzionální data.
</article>
